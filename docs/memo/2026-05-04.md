# 맛집 지도 

---

# ⚠️ Firebase API 키는 절대 Git에 올리면 안 돼요!

## 🚫 절대 올리면 안 되는 곳

```
❌ docs/memo/
❌ GitHub 레포지토리
❌ 공개된 문서 어디든
```

## ✅ 안전하게 보관하는 방법

### Option 1: 로컬 메모장 (추천!)

```
C:\Users\내이름\Documents\our-taste-map-keys.txt
또는
D:\개인자료\firebase-config.txt
```

### Option 2: 비밀번호 관리 앱

```
1Password, LastPass, Bitwarden 등
→ 안전하게 암호화됨
```

### Option 3: 프로젝트 폴더 (Git 제외)

```
our-taste-map/.env.local  ← .gitignore에 이미 포함됨
```

## 📝 올바른 사용법

### 1. .env.local 파일 만들기

프로젝트 루트에 .env.local 파일 생성:

```
our-taste-map/
├── .env.local  ← 여기 생성!
├── src/
├── package.json
└── ...
```

내용:

```
envVITE_FIREBASE_API_KEY=AIzaSy...
VITE_FIREBASE_AUTH_DOMAIN=our-taste-map.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=our-taste-map
VITE_FIREBASE_STORAGE_BUCKET=our-taste-map.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:...
```

### 2. .gitignore 확인

```
.env.local  ← 이미 있어야 해요! ✅
.env
```

확인 방법:

```bash
# .gitignore 파일 열어서
# .env.local 또는 .env가 있는지 확인
```

### 3. 코드에서 사용

src/firebase.ts (나중에 만들 파일):

```typescript
const firebaseConfig = {
  apiKey: import.meta.env.VITE_FIREBASE_API_KEY,
  authDomain: import.meta.env.VITE_FIREBASE_AUTH_DOMAIN,
  projectId: import.meta.env.VITE_FIREBASE_PROJECT_ID,
  storageBucket: import.meta.env.VITE_FIREBASE_STORAGE_BUCKET,
  messagingSenderId: import.meta.env.VITE_FIREBASE_MESSAGING_SENDER_ID,
  appId: import.meta.env.VITE_FIREBASE_APP_ID
};
```

## 🎯 지금 당장 할 것

1. Firebase에서 받은 config 복사
2. 안전한 곳에 저장 (택 1):

```
□ 로컬 메모장 (추천!)
□ 스크린샷 찍어서 개인 폴더
□ 비밀번호 관리 앱
```

3. memo에는 이렇게만 적기:

```markdown
# Firebase 설정 완료
- Firestore 생성 ✅
- Storage 생성 ✅  
- 웹 앱 등록 ✅
- API 키 받음 ✅ (로컬에 안전하게 보관)

※ Firebase config는 .env.local에 저장
```

---



---